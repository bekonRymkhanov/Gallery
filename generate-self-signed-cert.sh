#!/bin/bash

# Create nginx config directory if it doesn't exist
mkdir -p nginx/conf.d

echo "Nginx configuration directory created."
echo "For production deployment, consider adding SSL/TLS certificates."